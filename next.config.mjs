/** @type {import('next').NextConfig} */

const nextConfig = {
    basePath:'/verseify',
    assetPrefix:'/verseify',
    trailingSlash: true,
    
    swcMinify: true,
    reactStrictMode: true,
}

export default nextConfig;

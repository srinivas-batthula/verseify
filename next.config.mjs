/** @type {import('next').NextConfig} */

const nextConfig = {
    basePath:'/verseify',
    assetPrefix:'/verseify',
    trailingSlash: true,
    
    reactStrictMode: true,
}

export default nextConfig;
